# 外中断控制

## EFLAGS

![外中断控制](./image/eflag.png)

## 核心代码

```cpp
bool interrupt_disable();           //清除IF位，返回设置之前的值 
bool get_interrupt_state();         // 获得IF位
void set_interrupt_state(boot state)// 设置IF位  
```

与进程互斥有关系